spring.datasource.url=${datasource.url}
spring.datasource.username=${datasource.username}
spring.datasource.password=${datasource.password}
spring.datasource.continue-on-error=${datasource.continueOnError}
spring.jpa.hibernate.ddl-auto=${datasource.ddl-auto}
spring.kafka.consumer.group-id=${kafka.groupId}
spring.kafka.consumer.properties.spring.json.trusted.packages=org.go.together.dto