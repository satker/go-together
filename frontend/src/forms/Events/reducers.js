import {createEmptyResponse} from "../../App/utils/utils";

export const events = {
    findEvents: createEmptyResponse(false, []),
    parameters: createEmptyResponse(false, []),
    apartmentTypes: createEmptyResponse(false, []),
    languages: createEmptyResponse(false, [])
};