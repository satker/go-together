import {createEmptyResponse} from "App/utils/utils";
import {EDIT_MAIN_INFO_HOUSING_TYPES} from "../constants";

export const commonInfo = {
    housingTypes: createEmptyResponse(EDIT_MAIN_INFO_HOUSING_TYPES, [])
};